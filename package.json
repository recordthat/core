{
  "name": "@recordthat/core",
  "version": "0.0.12",
  "bin": {
    "@recordthat/core": "./bin/init.js"
  },
  "scripts": {
    "dev:server": "nodemon --watch src -e ts --exec \"npm run build:server && node dist/cli.js\"",
    "dev:web": "cd web && npm run dev",
    "build:server": "tsc",
    "build:web": "cd web && npm run build",
    "prepublishOnly": "npm run build:server && npm run build:web",
    "engine-validate": "npx ls-engines"
  },
  "dependencies": {
    "@fastify/cors": "^8.5.0",
    "@fastify/static": "^6.12.0",
    "fastify": "^4.29.0"
  },
  "engines": {
    "node": ">= 14.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "nodemon": "^3.1.9",
    "typescript": "^5.7.3"
  },
  "files": [
    "dist",
    "bin",
    "README.md",
    "web/out"
  ]
}
